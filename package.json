{
  "name": "codeweb",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "codeweb deterministic engine + regression suite",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\""
  }
}
