# This spike is source-only in git. Everything below is regenerated by `npm install && npm run spike`
# (see README.md) — keeping the repo lean while the tree-sitter tier is still exploratory. The
# adoption PR (on a GO decision) is where the matched grammar .wasm gets vendored for real.
node_modules/
grammars/*.wasm
out/
