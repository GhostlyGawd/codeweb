# reports/

Working audit reports from agent-run review programs — the inputs behind the fix
batches in the changelog. Each file is one audit's findings at the date it ran;
they are a paper trail, not user documentation. Fixes land in the code with tests,
and `FIXLOG.md` here records which findings were implemented when.
