{
  "name": "codeweb-spike-tree-sitter",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "QUARANTINED SPIKE — evaluates a tree-sitter parser tier (web-tree-sitter WASM). Isolated from the zero-dependency root package on purpose; promote only on an explicit go decision (see docs/backlog-ast-tree-sitter.md).",
  "scripts": {
    "spike": "node spike.mjs",
    "determinism": "node determinism.mjs"
  },
  "dependencies": {
    "@vscode/tree-sitter-wasm": "^0.3.1",
    "web-tree-sitter": "0.26.9"
  }
}
