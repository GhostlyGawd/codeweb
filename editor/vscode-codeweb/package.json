{
  "name": "codeweb-lens",
  "displayName": "codeweb CodeLens",
  "description": "Callers and blast radius above every mapped symbol, served from .codeweb/graph.json — the codeweb structure graph, in the editor.",
  "version": "0.1.0",
  "publisher": "codeweb",
  "license": "MIT",
  "engines": { "vscode": "^1.85.0" },
  "categories": ["Other"],
  "main": "./extension.js",
  "activationEvents": ["onStartupFinished"],
  "capabilities": {
    "virtualWorkspaces": false,
    "untrustedWorkspaces": { "supported": true, "description": "Read-only: renders numbers from .codeweb/graph.json; never executes workspace code." }
  },
  "contributes": {
    "commands": [
      { "command": "codeweb.openReport", "title": "codeweb: Open symbol in the interactive report" }
    ],
    "configuration": {
      "title": "codeweb",
      "properties": {
        "codeweb.lens.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Show codeweb CodeLens (callers · blast) above mapped symbols."
        },
        "codeweb.lens.minCallers": {
          "type": "number",
          "default": 0,
          "description": "Hide lenses for symbols with fewer than this many direct callers."
        }
      }
    }
  }
}
