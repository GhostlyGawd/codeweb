# Round-2 WS-E — advisors + leftovers (#41 #22 #23 #24 #26 #27 #28 #42)

Parent: `docs/specs/round2-plan.md` (E bar: reading-order @15.7k < 1 s, first-N byte-identical; break-cycles dense-SCC < 1 s, identical verdicts; overlap Signal-B ≥ 4× with identical candidate pair set; each gains a bench row). Builds only after WS-C lands (#16 shares `overlap.mjs`). Build order: **#41 → #22 → #23 → #24 → #26 → #27 → #28 → #42** — NULs first so `break-cycles.mjs`/`diff.mjs` stop classifying as binary and every later task can grep/review them. One commit (group) per finding; evidence (commands + numbers + sha) appends to `round2-evidence.md`.

Corpus for all measurements: `M=/tmp/cw-ws-e` (or scratchpad), built once —
```
node -e 'import("./bench/lib/loaded-corpus.mjs").then(m=>console.log(JSON.stringify(m.writeLoadedCorpus("/tmp/cw-ws-e/src",{files:680}))))'  # ~15.7k nodes; record exact count
node scripts/run.mjs /tmp/cw-ws-e/src --target e-loaded --out-dir /tmp/cw-ws-e/ws
git worktree add /tmp/cw-ws-e/base <WS-E-start-sha>   # pre-fix baseline binaries for identity/speed diffs
```
All timings: min of 3 runs. Identity checks: run baseline + new on copies of the same workspace, `cmp` outputs (determinism invariant: `SOURCE_DATE_EPOCH` byte-identity holds for every artifact writer touched).

## #41 — NUL bytes out of break-cycles.mjs / diff.mjs (CHORE, first)
- **T-41.1 (test first)** `tests/no-control-bytes.test.mjs` (new): for every `git ls-files '*.mjs'` file, read raw bytes and assert none is `< 0x09` (NUL…backspace; `\t`=0x09/`\n`/`\r` unaffected). Must go RED naming exactly `scripts/break-cycles.mjs` (3 bytes) and `scripts/diff.mjs` (1 byte).
- **T-41.2** Replace the 4 raw NULs with the `'\x00'` escape (same runtime string, so outputs stay byte-identical — the idiom of `graph-ops.mjs:13`/`extract-symbols.mjs:190`): `break-cycles.mjs:38` (comment: write the text `\x00`), `:42` `` `${f}\x00${t}` ``, `:45` `.split('\x00')`; `diff.mjs:44` `` `${o.kind}\x00${…}` ``. Verify: T-41.1 green; `grep -c fileCycles scripts/break-cycles.mjs` works (no "binary file"); break-cycles + diff stdout on a cyclic fixture `cmp`-identical to baseline; suite green.

## #22 — reading-order: budgeted greedy, live counters (PERF)
Files: `scripts/lib/reading-order.mjs`, `tests/reading-order.test.mjs`, `bench/all.mjs`, `bench/budgets.json`.
- **T-22.1 (property first)** In `reading-order.test.mjs`, seeded property (`tests/_proptest.mjs` prng/randomGraph, ≥200 cases, self-call edges + cycles included, scopes `all|file|symbol`): (a) **early-exit ≡ full prefix** — `readingOrder(g,{scope,budget:b})` equals `readingOrder(g,{scope,budget:Infinity}).slice(0,b)` for random b ∈ {0,1,…,n+2}; (b) **new ≡ oracle** — full order equals a frozen verbatim copy of today's greedy kept inside the test. Green today; it pins T-22.2.
- **T-22.2** Rewrite the loop keeping the exact selection order (`un` asc → `fanIn` desc → `id` asc — a strict total order, so scan order over `remaining` can never change the argmin; state this invariant in a comment):
  - live `un` counters: `un: Map(id→count)` init `calleesIn.get(id).size`; reverse-caller map `callersIn: Map(calleeId→[in-scope callers])` built once from `calleesIn`; on emit, decrement `un` for each not-yet-emitted caller — kills the per-candidate callee rescan;
  - `remaining` as array + position map, removal by **swap-pop** (O(1)); selection is one linear scan per emit;
  - early exit: `while (order.length < budget && remaining.length)` with `budget` clamped ≥ 0 (Infinity OK) — the greedy choice depends only on the emitted set, so first-N is byte-identical to the full order's prefix (the property proves it); drop the trailing slice;
  - `scopeIdsOf` symbol closure: replace `q.shift()` with an index-pointer queue (`for (let i=0;i<q.length;i++)`, the `impactOf` idiom).
- **T-22.3** Bench row: add `readingOrder: sh(S('reading-order.mjs'), [gpath, '--json'])` to `bench/all.mjs` advisorRuns (factor-gated; new `advisorFactorVsRegexBaselineMax.readingOrder` in budgets.json, ~3× headroom over set-time observed, noted in the comment) and record `readingOrderMs` in the `loaded` section.
- Evidence: `for i in 1 2 3; do time node scripts/reading-order.mjs /tmp/cw-ws-e/ws/graph.json --json >/dev/null; done` → min **< 1.0 s** (pre-fix ~76 s); identity: baseline worktree vs new, `cmp` JSON at default `--budget 40` and at `--budget 100000` (full order; baseline run once, ~76 s).

## #23 — break-cycles: SCC-induced pseudo-graph cut verification (PERF)
Files: `scripts/break-cycles.mjs`, `scripts/lib/graph-ops.mjs`, `tests/break-cycles.test.mjs`, `bench/lib/loaded-corpus.mjs`, `bench/all.mjs`, `bench/budgets.json`.
- **T-23.1 (seam)** Hoist the per-cycle candidate/verify logic into `graph-ops.mjs` as `export function cheapestCuts(graph)` returning today's `cycles` payload array; `break-cycles.mjs` keeps argv/IO/rendering only. Behavior-preserving (CB-DETERMINISTIC + a `cmp` of CLI stdout on the existing fixtures pins it). Also export `CYCLE_KINDS` (call|import|inherit|ref) from graph-ops and use it in `fileCycles`' filter and `createMergeSimulator` (both currently inline copies) — one truth for the kind set.
- **T-23.2 (property)** Seeded property (≥150 graphs): random multi-file graphs with edges of **all four kinds**, guaranteed cycles, including cycles **closed only by a ref edge** and pairs alive via ref after their structural cut. Assert `cheapestCuts(g)` deep-equals an oracle kept verbatim in the test = today's whole-graph path (`fileCycles` on `edges.filter(e => !rm.has(edgeKey(e)))`, same-key survival). Mutation check (documented in the test): temporarily counting witnesses from STRUCTURAL only must turn the property RED — proves the test bites on the ref subtlety. Plus a scenario test: `given a pair with 1 call + 1 ref edge, when its structural cut is tried, then the cycle survives and the next candidate is chosen` (exactly today's verdict).
- **T-23.3 (impl)** Inside `cheapestCuts`, per cycle: candidates and weights stay STRUCTURAL-only (byte-identical enumeration — **the subtlety: `STRUCTURAL` omits `ref` while `fileCycles` counts it**, so witness counts must NOT reuse STRUCTURAL); build `pairWitness: Map('f\x00t' → count)` from **all CYCLE_KINDS** edges with both files in the SCC (one pass; duplicate (from,to,kind) edges each count, matching `edgeKey`-set removal which removes every duplicate that the candidate also collected). Verify a candidate by: surviving pairs = every pair with `count − (pair===candidate ? candidate.weight : 0) > 0`; run the **same `fileCycles`** on the pseudo-graph (one node per file, one call edge per surviving pair — the `createMergeSimulator.cyclesOfPairs` precedent); cut works iff no surviving cycle has `cycleKey === key`. Why exact (state in a comment): removal only touches in-SCC pairs, so the outside graph is unchanged; a post-removal cycle among SCC files routing through an outside file would have put that file in the SCC pre-removal ("an outside router would itself be in the SCC"); SCCs only shrink under removal — so pseudo-graph same-key survival ⇔ whole-graph same-key survival, per candidate. Prototype: byte-identical verdicts, 0.16 s (~144×).
- **T-23.4 (bench + cyclic corpus)** New export in `bench/lib/loaded-corpus.mjs`: `writeCyclicCorpus(dir, {files=60, extraPairDeps=240, seed})` — seeded, deterministic: file i imports+calls file (i+1)%files (one files-wide SCC) plus random extra cross-file deps densifying it so **no single-pair cut breaks it** (the measured worst case). `bench/all.mjs`: map it into `ws/cyclic-ws`, run break-cycles + campaign `--json`, new `cyclic` payload section; gate that `cycles.count ≥ 1` (the machinery engaged — the acyclic-corpus lesson) and `cyclicBreakCyclesMsMax` (budgets.json, ~3× headroom, observed noted). Also add `breakCycles` to the self-map advisorRuns + budgets factor.
- Evidence: `node -e 'import("./bench/lib/loaded-corpus.mjs").then(m=>m.writeCyclicCorpus("/tmp/cw-ws-e/cyc",{files:60,extraPairDeps:240}))'`; map; min-of-3 `node scripts/break-cycles.mjs …/cyc-ws/graph.json --json` **< 1.0 s** (pre-fix ~23 s); `cmp` payload vs baseline worktree (byte-identical verdicts); campaign on the same graph recorded before/after.

## #24 — overlap LSH: kill the singleton-bucket tax (PERF)
Files: `scripts/overlap.mjs` (Signal B :243-263, Signal C :357-383), `scripts/lib/minhash.mjs`, `tests/type3-clones.test.mjs` or new `tests/minhash-buckets.test.mjs`, after WS-C's #16 lands.
- **T-24.1 (tier 1 — multi-occupancy-only sort; zero semantic risk)** During bucket insert, record a key in `multiKeys` the moment its bucket reaches length 2; sort/walk **only** `multiKeys` (singletons contribute nothing to the walk today — `ids.length < 2` continues). Visit order over multi buckets is the same sorted order ⇒ candidate pair set, `skippedBuckets`, and `buckets: buckets.size` all unchanged ⇒ artifacts byte-identical. Apply to B and C. Verify: `cmp` graph.json overlaps + overlap.md vs baseline on self-map and the 15.7k corpus (kills the ~1.4 s sort).
- **T-24.2 (property before tier 2)** Lift the banding+bucket+pair-enumeration walk into `lib/minhash.mjs` — `export function lshCandidatePairs(entries /* [id, items[]][] */, {K, bands, rows, bucketCap})` → `{pairs (sorted, deduped), buckets, skippedBuckets}` — and call it from both signals (B: 192/64/3; C: occurrence-expanded items, 128/32/4). Seeded property (≥200 cases, random item sets with forced collisions/empties): result deep-equals a string-key reference implementation kept verbatim in the test. Byte-identity of artifacts re-verified after the hoist.
- **T-24.3 (tier 2 — numeric band-hash keys, behind the property)** Inside `lshCandidatePairs`: per band, fold the row values into one uint32 (imul/fmix mixing, deterministic) and bucket in a `Map<number,…>` per band; each bucket stores its **first-insert row tuple**; a later insert compares tuples and on mismatch (true hash collision) **spills deterministically** to a per-band string-key side map — grouping stays exactly the string-key grouping, provably, not probabilistically (prototype: identical candidate pair set, ~8× on key build). The property (T-24.2) is the gate; tier 2 does not land while it is red.
- Evidence: min-of-3 `CODEWEB_TIMING=1 CODEWEB_WS=<ws-copy> node scripts/overlap.mjs` on the 15.7k corpus — `[overlap-timing] signal-B (twin enumeration)` mark **≥ 4×** faster vs baseline; `[run] overlap done in Xms` also recorded; artifacts `cmp`-identical. No new bench row needed (stage factors already gated via `stageFactorVsRegexBaselineMax.overlap` + `loaded.stageMs`).

## #26 — dup-check pool served from similar-index (PERF)
Files: `scripts/lib/shingles.mjs`, `scripts/lib/similar-index.mjs`, `scripts/lib/dup-check.mjs`, `scripts/find-similar.mjs`, `scripts/review.mjs`, `tests/dup-check.test.mjs`, `tests/find-similar.test.mjs`.
- **T-26.1 (characterization first)** Pin current `incrementalOverlap` outputs on fixtures: short bodies, a >400-line pool body, unreadable body, no-match. These must not change through T-26.2/3 (short-body cases byte-identical throughout).
- **T-26.2 (cap asymmetry — DECISION: cap the sidecar build too)** Today `buildSimilarIndex` shingles **uncapped** bodies (`sourceReader.bodyOf` has no cap) while dup-check + overlap cap at 400. Move the constant to the one home: `export const BODY_LINE_CAP = 400` in `lib/shingles.mjs`; import it in overlap.mjs, dup-check.mjs (delete both local copies), and apply it in `buildSimilarIndex` (slice body lines before shingling) **and in find-similar's live path** — so sidecar ≡ live stays true for long bodies (finding-15's own rationale: one answer per question, the capped one). Bump the sidecar `version` 1→2 in build + `loadSimilarIndex` so stale uncapped v1 sidecars are rejected, never served. Document the cap in both file headers.
- **T-26.3 (serve the pool)** `incrementalOverlap(graph, changedIds, {root, bodyOf, similarIndex})`: pool members with a sidecar record use stored `n` for the RATIO_FLOOR precut **before** any read/materialization; survivors intersect by iterating `rec.sh` against the candidate set (find-similar's :74-84 pattern — no Set build); per-node fallback to live shingling when the id is absent; **changed ids always shingle live** (their bodies are the new code under judgment). `review.mjs:86` passes `similarIndex: loadSimilarIndex(abs)` — stamp-stale ⇒ null ⇒ live path unchanged (fall back when stale; note: until WS-F's #25 regenerates sidecars on refresh, the fresh path mostly engages after full maps — correct either way). Tests: sidecar-fresh ≡ live byte-identical; stale stamp ⇒ identical to live; missing id ⇒ still found.
- Evidence (min-of-3, fresh sidecar): `node --input-type=module -e 'import {incrementalOverlap} from "./scripts/lib/dup-check.mjs"; import {loadSimilarIndex} from "./scripts/lib/similar-index.mjs"; import {readFileSync} from "node:fs"; const p="/tmp/cw-ws-e/ws/graph.json", g=JSON.parse(readFileSync(p,"utf8")); const id=g.nodes.find(n=>n.kind==="function").id; const t=Date.now(); incrementalOverlap(g,[id],{root:"/tmp/cw-ws-e/src",similarIndex:loadSimilarIndex(p)}); console.log(Date.now()-t+"ms")'` → **≤ ~60 ms** (pre ~505 ms); result equal to the live-path run.

## #27 — campaign: concurrent advisors + clone opt-out (PERF)
Files: `scripts/campaign.mjs`, `scripts/lib/campaign.mjs`, `tests/campaign.test.mjs`.
- **T-27.1 (characterization first)** CLI-level: campaign `--json` on a fixture with cycles + dups + orphans, payload pinned; stays byte-identical through T-27.2/3.
- **T-27.2** Replace the three sequential `spawnSync`s with async `spawn` + stdout collection wrapped in promises; `const [optimize, deadcode, breakCycles] = await Promise.all([...])` (top-level await). Preserve degrade-to-empty exactly: any spawn error / non-JSON stdout ⇒ null ⇒ the existing `|| {…}` defaults; child argv unchanged (`--json`, deadcode's `--all` passthrough). Wall → ~max(child).
- **T-27.3** `planCampaign(graph, {…, clone = true})`: `const g0 = normalizeGraph(clone ? structuredClone(graph) : graph)`. `campaign.mjs` passes `clone:false` (it owns the freshly-parsed graph; normalizeGraph's mutation is additive default-filling and `meta` is untouched, so the later `graph.meta?.target` read is safe — note this in a comment). All other callers keep the safe default (−260 ms at 15.7k).
- Evidence: min-of-3 `time node scripts/campaign.mjs /tmp/cw-ws-e/ws/graph.json --json >/dev/null` — pre ~2.6 s → **≈ max(child) + compose (~1.1 s class)**; payload `cmp`-identical to baseline; existing bench advisor row `campaign` (factor 3) simply improves.

## #28 — diff rename detection: indexed, memoized, capped-with-a-note (FIX)
Files: `scripts/diff.mjs`, `tests/diff.test.mjs`. (Lib-extraction itself is WS-F #33 — see shared-files.)
- **T-28.1 (characterization + cap-trip test first)** Pin `renamed[]` on fixtures (rename-in-place, move-with-name, sub-0.85 non-match, span-shape fallback). New RED test: with >200 removed or added, payload must carry an explicit marker — add `nodes.renameCheck = { skipped: true, removed, added, cap: 200 }` only when the cap trips (additive; absent otherwise) + one text-mode line; today it goes silent.
- **T-28.2** Inside a **pure top-level function** `detectRenames({before, after, bIx, aIx, nodesRemoved, nodesAdded, bReader, aReader})` (no ambient state — WS-F lifts it verbatim): `nOf`/`bodyOf` use the already-built `bIx.byId`/`aIx.byId` (drop both `g.nodes.find`); hoist `ob` + `structuralShingles(ob,3)` per oldId out of the inner loop; memoize `newId → structuralShingles` in a Map for the whole pass (each added node shingled once, not once per removed node); cap bodies at `BODY_LINE_CAP` lines (import from `lib/shingles.mjs`, the overlap precedent) before shingling. Long-body sims may change only for >400-line bodies (sanctioned — same answer overlap gives); add a test that an obvious >400-line rename still detects.
- Evidence: build `after.json` from the 15.7k graph with 195 relabeled nodes (small node script kept with the evidence); min-of-3 `node scripts/diff.mjs before.json after.json --json` — rename-detection overhead vs the 0-rename diff **≤ ~50 ms** (pre +498 ms); `renamed[]` identical to baseline.

## #42 — migration leftovers (CHORE; one tiny task each)
- **T-42.1** Drop dead `writeFileSync` imports: `overlap.mjs:18`, `build-report.mjs:11`, `coverage.mjs:10`, `refresh.mjs:11` (grep-verify zero remaining uses per file). Suite green.
- **T-42.2** `coverage.mjs:47`: `JSON.stringify(graph, null, 2)` → `JSON.stringify(graph)` (last pretty-printed graph.json, 20.6→13.0 MB). Extend `coverage.test.mjs`: annotated graph parses and is compact. (fragment.json pretty-print belongs to WS-D #19 — out of scope.)
- **T-42.3** Move the stranded jsdoc `cli.mjs:130-135` ("THE body reader") to directly above `sourceReader` (:194). Comment-only diff.
- **T-42.4** Mechanically re-indent `lib/import-resolve.mjs` to uniform 2-space (mixed 0/2 from the verbatim move): `git diff -w` must be empty; extract-equivalence suites green.
- **T-42.5** `lib/stats.mjs` (:30,:55,:73) + `lib/annotations.mjs` (:49): plain `writeFileSync` → `atomicWrite` (import from cli.mjs; bytes/formatting unchanged, incl. annotations' pretty-print; best-effort try/catch preserved). Extend `atomic-writes.test.mjs` to cover both writers.
- **T-42.6** `bench/results/scale-typescript.json`: add a top-level `"staleNote": "v0.9.0 pre-fix numbers; re-run deferred to WS-G #35's report-scale re-run"` — the actual receipt re-run lands in WS-G with #35, not here.
- **T-42.7** trend fast path: `run.mjs` gains `--stages through-overlap` (skip optimize+report; **never write the stage memo** on a partial run — the existence check is the belt, this is the brace); `trend.mjs` gitSnapshots passes it and reuses **one** workspace dir across commits (scan-cache reuse; worktree churn stays per-commit). `metrics()` reads only nodes/edges/domains/overlaps — all present at through-overlap. Tests: BDD in `trend.test.mjs` (given 2 commits, metrics equal pre-fix, one ws dir); `run.mjs` case: partial ws lacks report.html and a subsequent full run recomputes (memo not poisoned). Evidence: per-commit cost ~7 s → extract-dominated (~3 s class) on a 2-commit sample repo.

## Success criteria (the E bar, measured as above)
1. reading-order @~15.7k min-of-3 **< 1.0 s**, `--budget 40` and full-order outputs `cmp`-identical to baseline; property + suite green.
2. break-cycles on the dense cyclic corpus (60 files / 240 pair-deps) min-of-3 **< 1.0 s**, payload `cmp`-identical; property (incl. ref-closed cycles) green with the documented mutation check demonstrated RED once.
3. overlap Signal-B mark ≥ **4×** faster @15.7k with graph.json overlaps + overlap.md byte-identical (each tier separately).
4. dup-check 1-changed @15.7k ≤ ~60 ms sidecar-fresh, byte-equal to live; campaign ≈ max(child); diff 195-rename overhead ≤ ~50 ms with `renamed[]` identical.
5. Bench rows exist and gate: `node bench/all.mjs --gate` green with `readingOrder`/`breakCycles` advisor factors + the `cyclic` section (budgets set at ~3× set-time observed, values noted in the budgets comment — a changed number is a changed promise).
6. #41 lint test green repo-wide; #42 tasks each land suite-green; `npm test` ×2 + `check-consistency` green at workstream end; CHANGELOG entry per finding.

## Risk notes
- **#22** swap-pop reorders the scan — safe *only* because the comparator is a strict total order; never weaken the tie-break. Property test is the guard.
- **#23** kind-set drift is the trap (STRUCTURAL vs fileCycles' kinds): unify on exported `CYCLE_KINDS`; the mutation check proves the property would catch a ref-omitting witness table. `cheapestCuts` extraction changes no bytes of output (pinned before the optimization lands).
- **#24** tier-2 numeric collisions: first-insert triplet verification + deterministic spill keep grouping exactly string-key-equal; assert `buckets`/`skippedBuckets` equality too. Tier 2 reverts independently of tier 1.
- **#26** sidecar v2 bump is mandatory with the cap, else stale uncapped sets silently change long-body sims. Stamp covers graph bytes, not source drift — same documented contract find-similar already ships; fallback is always the live path.
- **#27** three concurrent children ≈ 3× peak child RSS at 16k-class graphs (acceptable; note in commit). `clone:false` is only for callers that own their graph — default stays safe.
- **#28** the line cap changes >400-line-body sims by design; the payload says so implicitly via BODY_LINE_CAP parity with overlap — long-body detection covered by test.
- **#42.7** a partial-stage workspace must never satisfy a later full-run memo (memo write skipped + output-existence check).
- Rollback: every finding is its own revert-clean commit (group); no task weakens an existing test.

## Shared-files coordination
- `overlap.mjs`: also edited by WS-C #16 — WS-E starts after WS-C lands; rebase T-24 onto the landed #16 before building.
- `diff.mjs`: WS-F #33 lifts diff's core into a lib — T-28.2's `detectRenames` must stay a pure, ambient-state-free top-level function so #33 moves it verbatim.
- `lib/shingles.mjs` becomes `BODY_LINE_CAP`'s one home in T-26.2; T-28.2 imports it — keep that ordering (#26 before #28).
- `run.mjs` (T-42.7) is also WS-D territory — coordinate; WS-D lands first per the plan's order, so rebase, don't fork.
