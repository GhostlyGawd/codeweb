# Checkout & Payment Flow Audit — codeweb

**Date:** 2026-07-23 · **Verdict: null — no checkout, payment, or donation surface exists in this repo.**

Read-only search pass over the full tree: grepped case-insensitively for `stripe|paddle|lemonsqueezy|paypal|patreon|ko-fi|opencollective|buymeacoffee|checkout|billing|payment|pricing|price|sponsor|funding|donate|buy|purchase` across all code, `site/` (all five content pages, nav, footer, templates — zero matches), `docs/` (built pages), `.github/` (workflows + actions), `package.json` (no `funding` field), and README.md. Every hit is incidental: `actions/checkout@v4` in CI workflows, `git checkout` in bench/script comments, test-fixture strings (`domain: 'billing'` in `tests/placement.test.mjs`, `it.price` in `tests/structural-clone.test.mjs`), and the example path `src/payments` in README.md:195. There is no `.github/FUNDING.yml` (the only FUNDING.yml in the tree is vendored benchmark corpus at `bench/corpus/axios/.github/FUNDING.yml` — axios's, not this project's), no Sponsors button, no donate/pricing/sponsor link anywhere on the static site, no payment SDK, no webhook handler, no license key, no cart. codeweb v0.9.0 is a free MIT developer tool with no money path at all — there is no flow from "buy" to "done" to walk, so the brief's seven lenses have no object and per its own rule this null result is the finding. The nearest thing to a checkout this product could ship is already specified: `REVENUE.md` (stage 3 of this playbook, repo root) maps what the future payment surface should look like — its §4.1 proposal (`.github/FUNDING.yml` + GitHub Sponsors profile, with GitHub handling checkout, VAT, and receipts, zero code in-repo) is the one-afternoon change that takes payment friction from infinite to solved, and a later hosted "Teams" tier is the point at which this audit would gain a real subject.
