# Round-2 WS-H — deep refactor finale: #40 orchestrator decomposition + #18b in-process hook extraction · hardened

Effort **L**. Runs **last** (per round2-plan build order). Shared-file locks: `scripts/extract-symbols.mjs` may be touched only after WS-D lands; `hooks/post-edit-diff.mjs` only after WS-F lands. Prior art: prior-#25 (docs/perf-quality-review-2026-07-21.md:169) extracted the pure halves — `lib/masking.mjs`, `lib/lang-rules.mjs`, `lib/import-resolve.mjs` (the factory template) with in-process tests (`tests/lang-rules.test.mjs`). #40 is prior-#25's tracked residual; #18b consumes it.

## H0 · Ground rules

- **Rebase gate (mandatory first step):** every line ref below is spec-time (commit 17a8225, pre-B..G). B/C/D edit this exact file (#8/#9/#12–#15 lang truth, #17 name-delta invalidation, #19 cache slimming, #21 enclosing() per-line index). Before coding each task, re-read `extract-symbols.mjs` at HEAD and **re-derive the free-variable audit — the METHOD binds, not the spec-time table**: enumerate every identifier in the function body, strike params/locals/builtins, classify the rest. Expected D-era drift, read against WS-D's own spec: #21's innermost-range index is built *inside* `deriveFileEdges` from `lines`+`ranges` (ws-d T-21.1 "built once at deriveFileEdges entry" — function-local, **no** new param/ctx field), and #17 (ws-d T-17.1) adds the candidate-identifier set to the **return value** (`cand`) — so the expected post-D free-identifier set is **unchanged** from the seven below; any eighth identifier the re-audit finds gets an explicit ctx field or lib-local import, never a reach-back.
- **Always green:** each task is one commit; full suite green before each; never weaken an existing test (converted tests keep identical assertions — only the transport changes).
- **Proof harness P1 — frozen-tree self-map byte-cmp (run after EVERY stage; ~1 s per self-map run ⇒ seconds per stage):**
  1. Once per workstream: `cp -a` the repo (minus `.git`, `.codeweb`, `node_modules`) to a scratch dir — `cp -a` preserves file *and directory* mtimes; `meta.sources`/`meta.dirs` embed size+mtime, so the tree must be bit- and stamp-frozen.
  2. **All `--out`/`--cache` paths live OUTSIDE the frozen tree** — `meta.dirs` stamps per-directory mtimes, so writing base.json into the tree flips `dirs` between runs and fails the cmp spuriously. Export `SOURCE_DATE_EPOCH=1753056000` for every P1/P2 command (plan's standing invariant; the extractor's fragment embeds no wall-clock today — verified, only build-report.mjs consumes it — the pin is drift-proofing, not a current dependency).
  3. Pre-stage commit: `node scripts/extract-symbols.mjs <frozen> --out <scratch>/base.json`; post-stage: same command → `cand.json`; **`cmp base.json cand.json` must be silent** (byte identity, ordering included — not just sorted-set equality). Post-D note: T-19.2 makes `--out` compact + skip-write; both runs sit on the same side of that change, so cmp semantics are unaffected.
  4. Warm variant post-stage: extract twice with `--cache <scratch>/c.json` into the same cache, `cmp` run2 vs the `--full` output.
- **Proof harness P2 — IE-EQUIVALENCE at full depth:** `SOURCE_DATE_EPOCH=1753056000 CODEWEB_IE_TRIALS=40 node --test tests/incremental-edges.test.mjs` — the knob and concurrent-subtest shape are WS-A T-6.2's (CI default 40; ~18–25 s at 4 cores post-#6; if A landed a different knob name, use it and note it in evidence). Post-D the generator includes T-17.4's `delsym`/`rensym`/`pkg` ops — H runs the extended semantics, never a trimmed one. Run after stages 1 and 5; local depth (10) suffices for stages 2–4.
- **Proof harness P3 — spawn accounting:** record at pre-H HEAD and after stage 4b: (a) total `grep -ro "runNode(" tests/ | wc -l` (spec-time: **278**, helpers.mjs's definition included); (b) the **extractor-invoking subset** `grep -rho "runNode(EXTRACT\|runNode(script('extract-symbols" tests/ | wc -l` (spec-time: **58**; verified: every `EXTRACT =` const in tests/ resolves to extract-symbols.mjs) — **the plan-§H ≥20 bar counts subset (b)**, since only extractor spawns are convertible by this workstream; plus one measured dynamic count (child launches during the converted tests, via a counting wrapper) for the ledger.

## T-40.1 · Stage 1: `createEdgeDeriver(ctx)` → `scripts/lib/edge-derive.mjs`

**Files:** new `scripts/lib/edge-derive.mjs`; `scripts/extract-symbols.mjs`; new `tests/edge-derive.test.mjs`.

**Approach.** Move `deriveFileEdges` (spec-time :685-799) verbatim into a factory, import-resolve style (`createImportResolver`, import-resolve.mjs:27, is the proven template — explicit injected ctx, renamed-at-destructure to dodge self-map bare-name false edges). Free-variable audit of the function body, **re-verified against 17a8225 by this review** — every identifier resolving outside its params/locals (H0's re-derive at HEAD supersedes the line numbers, not the method):

| free identifier | today (spec-time) | in the factory |
|---|---|---|
| `KEYWORDS` | import from lang-rules (:22) | lib imports directly |
| `byName` | module-global Map (:531) | `ctx.byName` |
| `pkgOf` | module fn closing over root+memos (:125) | `ctx.pkgOf` |
| `idFile` | module const (:49) | defined+exported by edge-derive.mjs; orchestrator imports it back (deletes :49) |
| `isTestFile` | import from graph-ops (:19) | lib imports directly |
| `LEGACY_FALLBACK` | env-derived const (:679) | `ctx.legacyFallback` (orchestrator keeps the env read) |
| `resolveFileMember` | resolver method (:546) | `ctx.resolveFileMember` |

Audited **not** free (briefing guesses that don't hold, re-checked): `files`; the alias/ns/class maps (per-call params, :685); the masked-text accessor (call-site concern — masking happens in the edge loop, :821). The edge loop with its cache replay (`reuseEdges`, :809-832) **stays in the orchestrator** — cache plumbing is orchestration per #40's fix text; likewise #17's delta/dirty-label computation (ws-d T-17.2) stays orchestrator — only the in-function `cand` collection and return field move with the body. Rule: every free identifier is either a lib-local import of a pure module or an explicit ctx field; zero reach-back into orchestrator module scope. Missed-injection tripwire: `LEGACY_FALLBACK` and `resolveFileMember` sit on conditional paths (no import-time ReferenceError), so the test list below deliberately exercises each branch — a missing ctx field fails loudly there and in P1, never silently. Signature and return shape (`{edges, hasModule, ambiguous}` + D's `cand` + any other D-era fields) preserved exactly.

**Criteria:** P1 silent; P2 full-depth green; `deriveFileEdges` callable in-process with a hand-built ctx.
**Tests:** `tests/edge-derive.test.mjs` (lang-rules.test.mjs pattern — no spawn, no tmpdir): alias > same-file > unique-in-package precedence; ambiguous drop; KEYWORDS skip; test-kind reclassification; rb/php cross-file-method bare-name filter (:715); `legacyFallback` flag behavior (both states); ns-alias member + `.call/.apply` chain (exercises `resolveFileMember`); self-call/self-definition suppression.
**Commit:** `refactor(engine): edge derivation behind createEdgeDeriver(ctx) in lib — finding #40`.

## T-40.2 · Stage 2: pub-API walk + typed-intent resolution join edge-derive.mjs

**Cohesion decision:** same lib, not new files — all three are global-resolution passes over the assembled node universe, each ~30-45 lines; import-resolve stays the import-binding home. **Cycle check done:** `lang-rules.mjs`, `graph-ops.mjs`, `import-resolve.mjs` each import only `node:path` — edge-derive importing lang-rules + graph-ops is acyclic; the decision stands. Two pure functions:

- `markPublicApi({ nodes, relFiles, pkgOf, sources, reExportEdges, readPkg })` (from :595-634): `readPkg(dir) → string|null` is injected so the lib does no fs; returns the **Set of node ids to stamp** — the orchestrator applies `n.pub = true` (order-safe: the walk never reads `pub` — verified at :618-633).
- `resolveTypedIntents({ intentsByFile, nodeIdSet, existingTriKeys })` (from :886-908): returns `{ edges, wired, dropped }`, appending keys to the caller-owned `existingTriKeys` (today's semantics); lib uses its own `idFile` + imported `isTestFile`. This mechanically eliminates #40's named shadowing smells (`rel` loop var vs `rel()` fn at :894; `files` local vs global at :896).

**Criteria:** P1 silent; IE local-depth green; both functions in-process-tested (deterministic iteration order pinned — the `[...keys].sort()` at :894 moves with the code).
**Commit:** `refactor(engine): pub-API walk + typed-intent resolution into lib/edge-derive — finding #40`.

## T-40.3 · Stage 3: main guard + `runExtract(opts)` — import becomes side-effect-free

**Files:** `scripts/extract-symbols.mjs`; new tests in `tests/run-extract.test.mjs`.

**Approach.** Repo idiom check done: run.mjs has no guard; the three hooks do — use **their** idiom verbatim: `if (process.argv[1] && resolve(process.argv[1]) === resolve(fileURLToPath(import.meta.url)))` (verified present at post-edit-diff.mjs:63, pre-edit-impact.mjs:109, session-brief.mjs:47). **Symlink caveat (verified):** the compare is lexical (`path.resolve`, no realpath) — under a symlinked invocation (npm bin shim) argv[1] is the symlink while `import.meta.url` is realpath'd, so main() would silently skip. Safe here because extract-symbols is **not** in package.json `bin` (only run.mjs/mcp-server.mjs are) and every in-repo caller spawns it by real absolute path; put that constraint in a comment on the guard line ("lexical compare — do not expose via bin without realpathSync on both sides"). No packed-install *execution* test exists (package-shape.test.mjs only runs `npm pack --dry-run` manifest checks) — the constraint is documented, not machine-enforced; any future WS adding a bin entry must revisit. Restructure:
- `export async function runExtract(opts)` (async — engine loads await) wraps everything from root resolution through cache write-back + banner; returns `{ fragment, banner }`. Cache write (incl. dirty-skip, `pub`-strip) stays **inside** runExtract — it is engine semantics the hook depends on. `--out`/stdout writes (incl. D's T-19.2 skip-write) move to `main()`.
- Defaults of `opts` equal today's CLI defaults (`ctags: true`, `engine: process.env.CODEWEB_ENGINE || null`, rest null/false) so `engineMode`/cache namespace match — warm caches survive; SCANNER_VERSION and cache format untouched.
- All exits become `throw new ExtractError(code, message)` with **byte-identical message text** (usage :67 / rules :77 / not-found :79 / empty tree :203-208 / zero symbols :939-943 — empty-target.test.mjs pins wording); `main()` prints and exits. After this, `process.exit` is unreachable from runExtract — pinned by the tests below (a stray exit kills the test harness process: loud).
- Module globals → runExtract locals: `files, nodes, byName, fileSyms, maskCache, oldCache/newCache/stampTier/cacheDirty/scanCount, manifestMemo/pkgMemo, ctagsBatch, sources, dynamicFiles, dispatchByFile, typedIntentsByFile/typedLangsSeen, alias tables, importEdges/edges + counters, astProbe/astLoadFailed, roleOverride`. Deliberate process-wide survivors, documented in a comment: `_tsEngineState`/`langEngines` (WASM parser memo — stateless; reloading per call would forfeit T-18b's win; the once-failed=null memo keeps per-process semantics). **Memoize the load *promise*, not the resolved value**, so two concurrent first-calls single-flight the WASM init (required before T-40.4 puts runExtract under #6's `{concurrency: 4}` subtests).

**Criteria (all four):** (a) **committed test SE-IMPORT-CLEAN** (not a one-off command): spawn `node -e "import('<fileURL>').then(()=>process.stdout.write('clean'))"` with cwd = empty tmpdir — exit 0, stdout exactly `clean`, empty stderr, tmpdir still empty (cli.mjs:19's pre-existing import-time EPIPE stdout handler is the one accepted side effect — hooks already carry it); (b) reentrancy — IE-TWO-RUNS: two sequential `runExtract` calls in one process on two different trees, each byte-equal (`JSON.stringify` compare) to a fresh CLI run; (c) **concurrency — IE-TWO-RUNS-CONCURRENT**: same two trees via `Promise.all`, same byte-equality (kills the interleaving/first-init race class before stage 4 makes trials concurrent); (d) P1 silent + CLI stderr/exit-code parity on the guard paths.
**Commit:** `refactor(engine): extract-symbols main guard + runExtract(opts); import is side-effect-free — finding #40`.

## T-40.4 · Stage 4a: starter trio of spawn→in-process test conversions

Chosen by reading which tests exercise only extraction/edge derivation. **Honest site counts (re-derived: only extractor-invoking `runNode` sites are convertible; `query.mjs` spawns in the same files are a different CLI — out of #40's scope, they stay):**
1. **tests/incremental-edges.test.mjs** — **2 extractor sites**, the trial inner loop (`extract`/`coldFull`, ~330-490 child launches at CI depth; the suite's dominant wall term pre-#6 at 48.9 s). Converts to `runExtract` **and gains IE-INPROC-PARITY**: once per run, `JSON.stringify(runExtract(...).fragment)` byte-equals the spawned CLI's stdout on the same tree — the CLI surface stays pinned while the loops go in-process. Convert whatever per-trial shape WS-A's #6 left (async helpers, concurrent subtests); the `edged N/M` assertions (IE-INCREMENTALITY :64-80 and D's T-17.4 additions) read the **returned `banner` string** — `edgedCount(warm.banner)` — assertions identical, transport changes.
2. **tests/call-apply-chain.test.mjs** — pure edge-derivation semantics (member-chain resolution), **1 extractor site / 3 launches** (verified: three tests share one `extract()` helper); the template for the family batch (writeTree → extract → hasEdge shape).
3. **tests/test-edges.test.mjs** — **1 extractor site** (its other 5 `runNode` sites spawn query.mjs and stay); still a required pick: test-kind reclassification lives inside `deriveFileEdges` (:721), so it doubles as seam coverage for T-40.1.

**Rejected candidate:** dup-check.test.mjs (briefing suggestion) — zero `extract-symbols` references (it spawns dup-check/review); nothing for `runExtract` to replace.

**Criteria:** all converted assertions identical; IE-EQUIVALENCE wall at CI depth drops ≥5× vs the post-#6 baseline (record actual); `cli-front-door`/`empty-target` stay spawn-based (CLI surface owners).
**Commit:** `test(extract): incremental-edges/call-apply-chain/test-edges run in-process — finding #40`.

## T-40.5 · Stage 4b: mechanical family batch — clears the plan's ≥20-site bar

Convert the import/edge-precision family (identical shape to call-apply-chain) — **extractor sites only** (query.mjs spawns in these files stay): `import-member-edges` (1), `class-usage-ref` (1), `import-default-export` (1), `import-anchor-precision` (1), `import-object-alias-precision` (1), `reference-edges` (1), `module-scope` (1), `inherit-edges` (1) = **8 sites**. Trio (4) + family (8) = 12 < 20, so **widen the batch** with same-shape single-extractor-site tests, all verified at spec time to assert nothing about banner/stderr content: `extract-symbols`, `extract-v7`, `signature`, `id-collision`, `python-imports`, `python-src-layout`, `python-docstring-mask`, `extract-rust`, `extract-go` (+9 → planned **21**). Spares if B/C/D changed any of those: `maskjs-regex-literals` (2), `freshness` (2), `coverage` (2), `reliance` (2).

**Partial-conversion rules:** (i) non-extractor spawns (query.mjs etc.) are untouched; (ii) an assertion on banner *text* reads the returned `banner`; an assertion on exit codes/stderr *semantics* keeps one spawn or moves to cli-front-door/empty-target — strength unchanged; (iii) cli-front-door and empty-target are never converted.

**Bar accounting (P3):** the plan-§H bar (**≥ 20 extractor-invoking spawn sites converted or obsoleted**) is absolute and stands — 58 such sites exist at spec time, so it is honestly clearable. The builder **recounts the subset ledger at pre-H HEAD** (B–D edit tests), converts until subset count ≥ 20, and reports the actual number converted + the before/after totals (278-baseline) in P3 — no file-level double-counting of non-extractor spawns. Dynamic child launches drop by ~350+ per CI-depth run (measured once, ledgered).
**Commit:** `test(extract): import/edge-precision family in-process (N spawn sites retired, N ≥ 20) — finding #40`.

## T-18b.1 · Stage 5: in-process extraction in the post-edit hook

**Files:** `hooks/post-edit-diff.mjs`; `tests/post-edit-diff.test.mjs`. Starts only after WS-F lands (shared-file lock) and T-40.3 is merged.

**Approach.** Per hook-fastpath-floor.md's named design, "baseline-fragment reuse rides the scan cache's stamp tier" — the splice against the baseline fragment IS calling the extractor with the warm `SCAN_CACHE_NAME` cache; no new splice code. **Symbol-set-changing edits (add/rename/delete) take WS-D #17's name-delta path *inside* runExtract — the hook contains zero splice/delta/invalidation logic of its own**, so the in-process fire runs exactly the machinery IE-EQUIVALENCE proved byte-identical; reimplementation is prohibited. Replace the child spawn (:46-47) with: **lazy** `await import('../scripts/extract-symbols.mjs')` inside `check()` *after* the `findTarget` guard (a static import would tax every inert fire; #18a's sidecar boot floor must not regress), then `after = (await runExtract({ path: t.root, cache, ...defaults })).fragment`. `check()` becomes async; the main guard awaits it. **Crash-safety ladder (fail-open, observable):** keep today's spawn code as `extractViaSpawn()`; env kill-switch `CODEWEB_HOOK_INPROC=0` forces it (the rollback lever); any throw from the lazy import or runExtract (incl. ExtractError) → one `extractViaSpawn()` attempt, then `return null`; on fallback, `bump(t.baseline, 'hookInprocFallbacks')` so silent divergence is ledger-visible, with zero stderr (fail-open silence). Everything downstream (structuralRegressions with WS-D's #18a baseline sidecar, stats bump, additionalContext emit) unchanged. Eliminated terms: child node boot, fragment stringify (child) + `JSON.parse` (hook, ~105 ms @16.8k), duplicate argv/probe. (The cache JSON.parse is NOT eliminated — it moves into the hook process; do not claim it.)

**Criteria:**
- **Perf (baseline-anchored, two-part gate):** baseline = WS-D's #18a evidence row — same corpus (16k-class `writeLoadedCorpus`), payload, and median-of-5 method; cite its sha. Binding: (i) **terms provably eliminated** — zero child processes during a no-change in-process fire (P3's counting wrapper), and median Δ vs the #18a row ≥ the child-boundary terms measured there (boot + fragment stringify/parse); (ii) expected landing **< 700 ms** median-of-5 (spec-time chain: 1,624 → −~325 #18a → −child-boundary; D's #19/#20/#21 floors also land in between). If (ii) misses while (i) holds, the gate becomes the honest floor: measured in-process `runExtract` wall + `regressionsAgainstSummary` wall on the same corpus + ≤ 75 ms hook overhead — record the real number; the post-18b residual must consist of delta-extract + after-side regression check only, and hook-fastpath-floor.md gets that number, not 700.
- **Verdict parity:** existing hook corpus — `post-edit-diff.test.mjs`, `hook-sidecar.test.mjs` (WS-D), `cache-unification.test.mjs` — passes with assertions unmodified (mechanical `await check(...)` only); byte-identical additionalContext/stderr on the regression fixtures; no-temp-file tripwire and "exactly SCAN_CACHE_NAME exists" still hold; dirty-skip still leaves cache bytes untouched on no-change (in-process write-back path).
- **New BDD scenarios:** "given a mapped target with a warm cache, when a no-change PostToolUse fires, then check() returns null and the cache file bytes are unchanged" (perf numbers live in the ledger, not flaky test asserts); "given the same regression fixture, when checked with `CODEWEB_HOOK_INPROC=0` and with the in-process path, then additionalContext is byte-identical" (path-parity, also proves the fallback lever works).
**Commit:** `perf(hooks): post-edit extraction in-process via runExtract (warm-cache splice) — finding #18b`.

## T-18b.2 · Evidence + floor-spec closure

Run P2 (IE full depth) + P1 once more at the final sha. Append to `docs/specs/round2-evidence.md` § WS-H: P1 cmp transcript per stage, IE wall before/after, P3 spawn counts (total + extractor subset + dynamic), hook median timings + corpus + sha + `hookInprocFallbacks` count observed during the test corpus (expected 0). Update `docs/specs/hook-fastpath-floor.md`: the "> 1.5 s → in-process extraction" revisit trigger is consumed — record the new measured floor (the honest number from T-18b.1, whichever gate branch bound) and set the next trigger (structuralRegressions residual stays WS-D's). CHANGELOG entries for #40 and #18b (plan constraint: entries land with the finding, and #40's entry names prior-#25 as now actually closed).
**Commit:** `docs(specs): WS-H evidence + hook-floor spec closure — findings #40 #18b`.

## Kill criteria & rollback (concrete tripwires, not effort-classes)

- **Stage 1:** if P1 is still non-silent after **3 distinct root-caused fix attempts** (different diagnosed causes, not retries), or stage 1's gate is still red at the end of a **second builder session** on it: revert T-40.1 WIP, abandon T-40.2, and land **stages 3-4 only** (T-40.3/4/5 — still delivers side-effect-free import, in-process testing, and the ≥20-site bar; T-40.4's edge tests then call `runExtract`, not the factory).
- **Any other stage:** its gate red across two builder sessions → revert that stage's WIP; later stages proceed only where independent (3/4 do not need 1/2; 5 needs 3 plus parity).
- **T-18b.1:** proceeds on stage 3 alone **iff** IE-INPROC-PARITY holds at CI depth; if parity is red after **2 distinct root-caused fixes**, #18b joins the residual (the `CODEWEB_HOOK_INPROC=0` lever means a landed-then-doubted 18b is also revertible without code churn).
- Record the honest residual (edge-deriver factory ± #18b) in the evidence ledger and CHANGELOG — the #25 lesson: track the residual explicitly, never call the finding done. Rollback unit is one commit per task; libs are additive files, so reverts are clean.
